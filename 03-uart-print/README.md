# 03-uart-print

Prints debug messages to a serial console via UART. Connect a Debug Probe to see the output.

## What you'll learn

- UART0 initialization (baud rate, 8N1)
- GPIO pin function selection for UART TX/RX
- Sending bytes and strings over UART
- Debug Probe wiring for serial output
